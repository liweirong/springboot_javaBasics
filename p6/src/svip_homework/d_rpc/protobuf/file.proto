syntax = "proto3";
//指定版本信息，不指定会报错;

// package声明符 proto 文件包名
package protocol;

option go_package = "protocol";
//option java_package 生成 proto 文件所在包路径，一般来说是和文件包名一致就可以
option java_package = "com.kone.pbdemo.protocol";

message File {
  string name = 1;
  int32 size = 2;
}